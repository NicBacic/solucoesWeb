language: ruby
rvm:
 - 2.2

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS sptrans_test;'
  - mysql -e "GRANT ALL PRIVILEGES ON *.* TO 'travis'@'localhost';"
services:
  - mysql

dist: precise

